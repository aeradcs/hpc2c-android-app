<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.sscc.ssd.hpccloud">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hpccloud">
        <activity android:name=".LoginPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ApplicationsPageActivity"></activity>
        <activity android:name=".DocumentsPageActivity"></activity>
        <activity android:name=".JobsPageActivity"></activity>
        <activity android:name=".UserInfoPageActivity"></activity>
        <activity android:name=".UserProfileMainPageActivity"></activity>

    </application>

</manifest>